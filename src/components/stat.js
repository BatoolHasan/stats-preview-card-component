const Stat = ({heading, subheading})=>{

    return (
        <div className="stat">
            <h2>{heading}</h2>
            <h3>{subheading}</h3>
        </div>
    );

}
export default Stat;